// Example of handling button click event for updating order status
document.querySelectorAll('button').forEach(button => {
    button.addEventListener('click', function() {
        alert('Order status updated!');
    });
});
